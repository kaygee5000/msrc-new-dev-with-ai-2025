export const metadata = {
  title: 'Contact MSRC',
  description: 'Get in touch with the Monitoring System for Responsible Choices team',
};

export default function ContactLayout({ children }) {
  return children;
}
